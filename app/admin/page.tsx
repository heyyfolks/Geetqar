'use client'
import {useState} from 'react'
export default function Admin(){const [file,setFile]=useState<File|null>(null);return <main className="min-h-screen bg-black px-6 py-24 text-white md:px-12"><div className="mx-auto max-w-3xl"><div className="text-[10px] tracking-[.4em] text-gold">GEETQAR / PRIVATE CONTROL</div><h1 className="mt-4 text-5xl font-bold">Artist Admin</h1><p className="mt-4 text-sm leading-7 text-white/40">This surface is a UI shell. Production authentication and signed upload endpoints must be enabled before exposing it publicly.</p><section className="glass mt-10 p-7"><h2 className="text-xl font-semibold">Upload original master</h2><p className="mt-2 text-xs text-white/35">WAV / FLAC · private storage · signed playback URLs</p><input className="mt-6 block w-full text-sm" type="file" accept="audio/wav,audio/x-wav,audio/flac,audio/x-flac" onChange={e=>setFile(e.target.files?.[0]||null)}/>{file&&<p className="mt-4 text-xs text-gold">Selected: {file.name}</p>}<button disabled={!file} className="mt-6 border border-gold/50 px-5 py-3 text-[10px] tracking-[.25em] text-gold disabled:opacity-30">GENERATE SECURE UPLOAD</button></section></div></main>}
