'use client'
import { useEffect, useState } from 'react'
import { ChevronUp, MessageCircle, Send } from 'lucide-react'
import { supabase, voteForQueueItem } from '@/lib/queue'
type Item={id:string;track_id:string;username:string;votes:number;status:string}
export function Jukebox({tracks}:{tracks:string[]}){const [items,setItems]=useState<Item[]>([]);const [name,setName]=useState('');const [message,setMessage]=useState('');const [chat,setChat]=useState<{username:string;text:string}[]>([]);useEffect(()=>{const db=supabase();db.from('queue_items').select('*').eq('status','queued').order('votes',{ascending:false}).then(({data})=>setItems(data||[]));const channel=db.channel('geetqar-jukebox').on('postgres_changes',{event:'*',schema:'public',table:'queue_items'},()=>db.from('queue_items').select('*').eq('status','queued').order('votes',{ascending:false}).then(({data})=>setItems(data||[]))).subscribe();return()=>{db.removeChannel(channel)}},[]);const add=(track:string)=>{if(!name.trim())return;supabase().from('queue_items').insert({track_id:track,username:name.trim()}).then(()=>{})};const vote=(id:string)=>{const voter=localStorage.getItem('geetqar-voter')||crypto.randomUUID();localStorage.setItem('geetqar-voter',voter);voteForQueueItem(id,voter).catch(()=>{})};const send=()=>{if(name.trim()&&message.trim()){setChat(c=>[...c,{username:name.trim(),text:message.trim()}]);setMessage('')}};return <div className="grid gap-5 lg:grid-cols-[1fr_1fr]"><div className="glass p-6"><div className="flex items-center justify-between"><div><span className="text-[9px] tracking-[.35em] text-gold">COMMUNITY JUKEBOX</span><h3 className="mt-2 text-2xl font-semibold">Vote the next sound.</h3></div><div className="rounded-full border border-gold/20 px-3 py-1 text-[9px] text-gold">LIVE</div></div><input value={name} onChange={e=>setName(e.target.value)} placeholder="Your username" className="mt-6 w-full border-b border-white/10 bg-transparent py-3 text-sm outline-none focus:border-gold"/><div className="mt-5 space-y-2">{tracks.map(t=><button key={t} onClick={()=>add(t)} className="w-full border border-white/10 p-3 text-left text-xs transition hover:border-gold/50">+ Add {t}</button>)}{items.map(i=><div key={i.id} className="flex items-center gap-3 border border-white/10 p-3"><div className="flex-1"><div className="text-sm">{i.track_id}</div><div className="text-[9px] text-white/30">added by {i.username}</div></div><button onClick={()=>vote(i.id)} className="flex items-center gap-1 text-gold"><ChevronUp size={16}/>{i.votes}</button></div>)}</div></div><div className="glass flex min-h-[360px] flex-col p-6"><div className="flex items-center gap-2 border-b border-white/10 pb-4"><MessageCircle size={16} className="text-gold"/><span className="text-[9px] tracking-[.35em]">LIVE DISCUSSION</span></div><div className="flex-1 space-y-3 overflow-auto py-5">{chat.map((c,i)=><div key={i}><span className="text-xs text-gold">{c.username}</span><p className="text-sm text-white/60">{c.text}</p></div>)}{!chat.length&&<p className="text-sm text-white/25">The room is quiet. Start the conversation.</p>}</div><div className="flex gap-2 border-t border-white/10 pt-4"><input value={message} onChange={e=>setMessage(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Say something about the music…" className="min-w-0 flex-1 bg-transparent text-sm outline-none"/><button onClick={send} className="text-gold"><Send size={17}/></button></div></div></div>}
